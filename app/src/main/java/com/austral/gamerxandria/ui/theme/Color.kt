package com.austral.gamerxandria.ui.theme

import androidx.compose.ui.graphics.Color

// Dark theme colors (original)
val BackgroundDark = Color(0xFF1A1411)  // Dark background color
val CardBackgroundDark = Color(0xFF2A2422)  // Slightly lighter background for cards
val AccentPurpleDark = Color(0xFF493077)    // Purple accent from hint background
val ButtonRedDark = Color(0xFFC11B3B)       // Red/burgundy color from FAB
val TextWhiteDark = Color.White             // White text
val TextGrayDark = Color.Gray               // Gray text for secondary information

// Navigation tab colors - Dark
val ActiveTabColorDark = Color(0xFF5D3F3F)  // Brown accent for active tab
val InactiveTabColorDark = Color(0xFF483434) // Darker shade for inactive tabs

// Status colors - Dark
val StatusWrongDark = Color(0xFFEC7C7C)     // Red for wrong guesses
val StatusNearDark = Color(0xFFECD07C)      // Yellow for near guesses
val StatusCorrectDark = Color(0xFF8DEC7C)   // Green for correct guesses

// Error colors - Dark
val ErrorBackgroundDark = Color(0xFFF37272) // Light red background
val ErrorTextDark = Color(0xFFF84040)       // Darker red text

// Light theme colors (new)
val BackgroundLight = Color(0xFFFFEFEF)     // Light cream background
val CardBackgroundLight = Color(0xFFEFEBE6)  // Slightly darker card background
val AccentPurpleLight = Color(0xFF6E48B0)    // Brighter purple accent
val ButtonRedLight = Color(0xFFD93251)       // Brighter red button
val TextDarkLight = Color(0xFF2D2926)        // Dark text for light mode
val TextGrayLight = Color(0xFF7D7873)        // Gray text for secondary information

// Navigation tab colors - Light
val ActiveTabColorLight = Color(0xFFD05B5B)  // Brown accent for active tab
val InactiveTabColorLight = Color(0xFFD3BFB0) // Lighter shade for inactive tabs

// Status colors - Light
val StatusWrongLight = Color(0xFFE85C5C)     // Red for wrong guesses
val StatusNearLight = Color(0xFFE8B85C)      // Yellow for near guesses
val StatusCorrectLight = Color(0xFF6BCC57)   // Green for correct guesses

// Error colors - Light
val ErrorBackgroundLight = Color(0xFFFFCDCD) // Light red background
val ErrorTextLight = Color(0xFFE62020)       // Red text

// Current theme colors (these will be set based on dark/light theme)
val Background = BackgroundDark
val CardBackground = CardBackgroundDark
val AccentPurple = AccentPurpleDark
val ButtonRed = ButtonRedDark
val TextWhite = TextWhiteDark
val TextGray = TextGrayDark

// Navigation tab colors
val ActiveTabColor = ActiveTabColorDark
val InactiveTabColor = InactiveTabColorDark

// Status colors
val StatusWrong = StatusWrongDark
val StatusNear = StatusNearDark
val StatusCorrect = StatusCorrectDark

// Error colors
val ErrorBackground = ErrorBackgroundDark
val ErrorText = ErrorTextDark

// Original Material You colors (keeping for reference)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
